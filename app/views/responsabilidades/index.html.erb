 <!-- Navigation -->
     <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
        <div class="container">
            <span class="navbar-brand text-white">Resposabilidades</span>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarsExampleDefault">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <%= link_to "Home", root_path, class: "nav-link text-white" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to 'Funcionários', funcionarios_path, class:"nav-link text-white" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Cargos", cargos_path, class: "nav-link text-white" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Dependentes", dependentes_path, class: "nav-link text-white" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Responsabilidades", responsabilidades_path, class: "nav-link text-white" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Relatórios", relatorio_path, class: "nav-link text-white" %>
                    </li>
                </ul>
            </div>
        </div>
    </nav> 

<br><br><br>

<div class="container">
  <div class="row col-md-12 col-md-offset-2 custyle">

<p id="notice"><%= notice %></p>

<h1>Responsabilidades</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Observacoes</th>
      <th>Funcionarios</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @responsabilidades.each do |responsabilidade| %>
      <tr>
        <td class="col 5"><div class="row col 12"><%= responsabilidade.nome %></div></td>
        <td class="col 5"><div class="row col 12"><%= responsabilidade.observacoes %></div></td>
        <% if responsabilidade.funcionarios.any? %>
        <td>
          
         <% responsabilidade.funcionarios.each do |funcionario| %>

          <%= funcionario.nome %>
         <%#= responsabilidade.funcionarios.nome %>

         <%end%>

        </td>

        <% else %>

          <td></td>

        <% end %>
        <td class="col 2"><div class="row col 10"><%= link_to 'Show', responsabilidade, class: "btn btn-info btn-s" %></div></td>
        <td class="col 2"><di %></div></td>
<td class="col 2"><div class="row col 10"><%= link_to 'Edit', edit_responsabilidade_path(responsabilidade), class: "btn btn-warning btn-s" %></div></td>
        <td class="col 2"><div class="row"><%= link_to 'Destroy', responsabilidade, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-s" %></div></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Responsabilidade', new_responsabilidade_path, class: "btn btn-primary btn-s pull-right" %>

</div>
</div>
